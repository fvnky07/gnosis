{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "fs",
  "description": "Filesystem access for user-selected vault directories. Grants read/write access to $HOME/** and /Volumes/** so vaults located anywhere under the user's home directory or on external volumes are reachable. This broad scope is intentional for the user-picks-vault story; tighten once Tauri supports dynamic capability scoping.",
  "windows": ["main"],
  "permissions": [
    "fs:default",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-read-dir",
    "fs:allow-mkdir",
    "fs:allow-exists",
    "fs:allow-remove",
    "fs:allow-rename",
    {
      "identifier": "fs:scope",
      "allow": [
        { "path": "$HOME/**" },
        { "path": "/Volumes/**" }
      ]
    }
  ]
}
